import React from 'react';
import { connect } from 'react-redux';

import {} from '../redux/upload'

const Upload = (props) => {

}

const mapStateToProps = state => {
    return {

    };
  };

  const mapDispatchToProps = dispatch => {
    return {

    };
  };

  export default connect(
    mapStateToProps,
    mapDispatchToProps
  )(
    Upload
  );
